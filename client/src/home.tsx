import Layout from "./layout";

export const Home = () => {
    return (
        <Layout>
            <div className="bg-slate-200 w-full h-full flex items-center justify-center">home</div>
        </Layout>
    );
};
