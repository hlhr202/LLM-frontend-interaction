import Layout from "./layout";

export const Users = () => {
    return (
        <Layout>
            <div className="bg-slate-800 w-full h-full flex items-center justify-center text-slate-50">
                users
            </div>
        </Layout>
    );
};
